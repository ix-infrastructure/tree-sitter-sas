# tree-sitter-sas
`tree-sitter-sas` is a standalone, open-source tree-sitter grammar for the SAS programming language. It enables any tool that understands the tree-sitter ecosystem (editors, static analyzers, code intelligence systems) to parse SAS source code into a concrete syntax tree.
